Optional hero background (GitHub Pages)

1) Video (recommended short loop, under ~8 MB for faster load):
   - Save as: hero.mp4
   - Optional poster image: hero-poster.jpg

2) If you do not add hero.mp4, the site still shows an animated gradient
   entrance — no action required.

Place files in this folder (same directory as this README).
